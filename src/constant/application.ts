export enum EAplicationEnvironment {
    DEVELOPMENT = 'development',
    PRODUCTION = 'production',
    TEST = 'test'
}
export const Roles = {
    ADMIN: 'admin',
    CUSTOMER: 'customer'
} as const
